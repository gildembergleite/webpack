/* eslint-disable no-undef */
import Heading from './components/heading/index'

const heading = new Heading()
heading.create('Star Wars')

console.log(VERSION)
console.log(PORT)
console.log(process.env.YOUR_API_KEY)
